version: "3.1"

services:
  nginx:
    image: nginx:latest
    volumes:
      - ./:/var/www/
      - ./docker/nginx/conf.d/:/etc/nginx/conf.d/
    ports:
      - "8876:80"
    depends_on:
      - app
    container_name: nginx_welotochka
    networks:
      - welotochka_network
    deploy:
      resources:
          limits:
            memory: 2048M
          reservations:
            memory: 2048M
    
  app:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
    volumes:
      - ./:/var/www/
    depends_on:
      - db
    container_name: app_welotochka
    networks:
      - welotochka_network

  db:
    image: mysql:8.0
    restart: always
    volumes:
      - ./tmp/db:/var/lib/mysql
    environment:
        MYSQL_DATABASE: welotochka_db
        MYSQL_ROOT_PASSWORD: root
    ports:
      - 8101:3306
    command: mysqld --character-set-server=utf8 --collation-server=utf8_unicode_ci
    container_name: db_welotochka
    networks:
      - welotochka_network

  phpmyadmin:
    container_name: pma_welotochka
    image: phpmyadmin/phpmyadmin
    ports:
      - 8081:80
    networks:
      - welotochka_network
    
networks:
  welotochka_network:
    driver: bridge